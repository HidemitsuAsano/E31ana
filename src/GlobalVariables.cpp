// GlobalVariables.cpp

#include "GlobalVariables.h"

ClassImp(GlobalVariables)

// + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + //
GlobalVariables::GlobalVariables()
  : TObject()
{
}

// + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + -- + //
GlobalVariables::~GlobalVariables()
{
}
